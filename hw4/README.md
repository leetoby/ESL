In the problems, you can download these file to riscv platform and run the following instructions:

$ cd $RISCV_DIR/riscv-vp/vp/build

$ make install

$ cd $RISCV_DIR/riscv-vp/sw/basic-sobel

$ make

$ make sim
